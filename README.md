# SKN18-3rd-3Team

# coupang_faq.csv
# 쿠팡 데이터.csv파일

# coupang_fna.py(크롤링 파일)
# 만약에 본인이 돌려보고 싶으면 오류가 날수있음(대부분 크롬 및 웹페이지 버전이 달라서 오류(맞춰야함))
# click_all_load_more_buttons() 함수 추가 - 더보기 버튼을 자동으로 찾아서 계속 클릭
# 최대 50번까지 클릭 시도 - 모든 FAQ가 로드될 때까지 반복
# 다양한 선택자 지원 - "더보기", "더 보기", "more", "More" 등 다양한 텍스트 인식
# 전체 수집 모드로 변경 - 더 이상 10개 제한 없음

# fnq.py(래그+streamlit)
# 파일안에 주석으로 설명 되어 있음
# 순서중 1번의 경우에는 래그 만들 시 수시로 변경 테스트를 해야할 것들을 위로 올려 놨음
# (각 순서에 있는 사이즈,모델등에 대한 빠른 변경을 위함)
# 또한 모델을 설치를 안함(속도가 매우 느림)-> groq사용함 
# !!반드시 .env 파일만들어서 GROQ_API_KEY="본인 key넣으면 됨"
# 추출 모델은 여러개를 사용해 봤는데 가장 적합한 것을 적용(본인이 다른것들로 실험해봐도됨)

# requirements.txt
# 그대로 설치하면되는데 만약에 실행시 설치가 안된게 있으면 에러코드에 나와있는거 설치하면 됨
# (전부 설치되게 해놨는데 혹시나 해서 써놓음)

# 기타
# 파이썬 3.13버전이고
# 래그 순서 중 1번을 제외(위에 설명)한 나머지는 간단하게 적용 함
# python run coupang_fna.py->쿠롤링 실행
# strealit run fnq.py->streamlit 실행(테스트 질문+응답은 코딩안함->이미 확인해서 지우고 밑에 streamlit만듬)
# 추가 문의있을 시 별도 연락 바람람